import { PlaceholderScreen } from '@components/ui/PlaceholderScreen';

export default function HomeScreen() {
  return (
    <PlaceholderScreen
      module="Hub Central"
      title="Inicio"
      subtitle="Accesos rápidos y actividad — próximamente"
    />
  );
}