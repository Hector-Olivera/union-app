import { PlaceholderScreen } from '@components/ui/PlaceholderScreen';

export default function StoreScreen() {
  return (
    <PlaceholderScreen
      module="Módulo"
      title="Tienda"
      subtitle="Compras + intercambios — próximamente"
    />
  );
}